# MORTIS
MORTIS is a local, user-owned, cryptographically accountable digital self-destruct orchestrator that runs entirely on the user's machine. It maintains an always-current, encrypted inventory of a user's complete digital footprint and, when triggered by any of several configurable mechanisms, executes a precisely choreographed sequence
